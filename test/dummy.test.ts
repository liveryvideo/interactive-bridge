import { describe, expect, it } from 'vitest';

describe('This is just a dummy', () => {
  describe('but everything', () => {
    it('should be tested', () => {
      expect(true).to.equal(true);
    });
  });
});
